const mongo = require('../mongo');

function coinVal(num) {
    return num = 1
};

function monthAmt(amt) {
    return amt = logs.month.uses
};

module.exports.coinVal = coinVal;
module.exports.monthAmt = monthAmt;